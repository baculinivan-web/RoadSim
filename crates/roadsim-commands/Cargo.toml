[package]
name = "roadsim-commands"
description = "Atomic typed command path for the RoadSim Design Model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
roadsim-domain = { version = "0.1.0", path = "../roadsim-domain" }
roadsim-types = { version = "0.1.0", path = "../roadsim-types" }

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
