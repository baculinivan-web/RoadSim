[package]
name = "roadsim-geometry"
description = "Deterministic local-metric geometry kernel for RoadSim"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
libm.workspace = true
roadsim-domain = { version = "0.1.0", path = "../roadsim-domain" }
roadsim-types = { version = "0.1.0", path = "../roadsim-types" }

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
